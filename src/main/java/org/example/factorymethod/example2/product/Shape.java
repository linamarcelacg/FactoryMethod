package org.example.factorymethod.example2.product;

//It can be abstract or interface.Most of the time is interface. Abstract if you need attributes and/or default method

public interface Shape{
    int area(int width, int high);
}
